using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Instructs : MonoBehaviour {
 /*   public Texture2D slideOne, slideTwo, slideThree,slideFour,slideFive;
    int index = 0;
    private SteamVR_Controller.Device controller;
    // Use this for initialization
    void Start () {
        if ((controller.GetPress(SteamVR_Controller.ButtonMask.Touchpad)){
            if (index == 0)
            {
                GetComponent<Texture2D>() = slideOne;

            }
            else if (index == 1)
            {
                GetComponent<Renderer>().material = slideOne;

            }
            if (index == 2)
            {
                GetComponent<Texture2D>().sprite = slideOne;

            }
            if (index == 3)
            {
                GetComponent<Texture2D>().sprite = slideOne;

            }
            if (index == 4)
            {
                GetComponent<Texture2D>().sprite = slideOne;

            }

        }
    }
}
	
	// Update is called once per frame
	void Update () {
 */     
}
